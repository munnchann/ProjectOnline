@*@using PagedList.Mvc*@
@{
    ViewBag.Title = "ViewCategory";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="card">
    <div class="card-header card-header-primary">
        <h4 class="card-title">Category</h4>

    </div>
    <div class="card-body">
        <form action="@Url.Action("AddCate")" method="post" class="form">
            <div class="row">

                <div class="col-md-12">
                    <div class="form-group">
                        <label class="bmd-label-floating"> Name Category</label>
                        <input id="Name_cate" name="Name_cate" type="text" class="form-control">
                    </div>
                </div>

            </div>



            <button type="submit" class="btn btn-primary pull-right">Save</button>
            <div class="clearfix"></div>
        </form>
    </div>
</div>
<div class="col-md-12">
   @* @{var view = ViewData["ViewCate"] as PagedList.IPagedList<CDstore.Models.Category>;*@
        <div class="card">
            <div class="card-header card-header-primary">
                <h4 class="card-title ">Category Table</h4>


            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">
                        <thead class=" text-primary">
                        <th>
                            ID
                        </th>
                        <th>
                            Name Category
                        </th>

                        <th>
                        </th>
                        </thead>
                        <tbody>



                          @*  @foreach (var item in view)*@
                            
                                <tr>
                                    <td>
                                        
                                    </td>
                                    <td>
                                        
                                    </td>

                                    <td class="text-primary">
                                        <a href="@Url.Action("EditCate", "Category")" class="btn btn-primary pull-right btn-sm">Edit</a>
                                        <a href="@Url.Action("DeleteCate", "Category")" class="btn btn-primary pull-right btn-sm">Delete</a>
                                    </td>
                                </tr>
                            


                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        @*<div class="row">
            <div class="col-sm-12">
                <div class="pagination font-alt">
                    <div class="numberpage">Page @(view.PageCount < view.PageNumber ? 0 : view.PageNumber) / @view.PageCount</div>
                    <div class="page">
                        @Html.PagedListPager(view, page => Url.Action("ViewCate",
                                       new { page }))
                    </div>
                </div>
            </div>
        </div>*@
    


</div>
<style>
    .pagination {
        display: flex;
        flex-direction: row;
        justify-content: space-around;
    }

        .pagination > li {
            width: 25px;
            height: 25px;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            border: 1px solid;
            border-radius: 10%;
            background-color: rebeccapurple;
        }

    .page {
        width: 350px;
    }

    .numberpage {
        width: 155px;
        height: 25px;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        float: inherit;
    }
</style>


