@*@using PagedList<ShopOnline..Models.Customer>*@
@{
    ViewBag.Title = "ViewCus";
    Layout = "~/Areas/Admin/Views/Shared/_LayoutAdmin.cshtml";
}

<div class="col-md-12">
    @*@{var view = ViewData["ViewCus"] as PagedList.IPagedList<ShopOnline.Models.Customer>;*@
        <div class="card">
            <div class="card-header card-header-primary">
                <h4 class="card-title ">Customer Table</h4>

            </div>
            <div class="card-body">
                <div class="table-responsive">
                    <table class="table">
                        <thead class=" text-primary">
                        <th>
                            ID
                        </th>
                        <th>
                            Name
                        </th>
                        <th>
                            Email
                        </th>
                        <th>
                            Address
                        </th>
                        <th>
                            Phone
                        </th>

                        <th>
                            Password
                        </th>
                        <th>
                        </th>
                        </thead>
                        <tbody>



                            @*@foreach (var item in Model)*@
                            
                                <tr>
                                    <td>
                                        @*@item.CusID*@
                                    </td>
                                    <td>
                                        @*@item.CusName*@
                                    </td>
                                    
                                    <td>
                                        @*@item.Address*@
                                    </td>
                                    <td>
                                        @*@item.Phone*@
                                    </td>

                                    <td>
                                       @* @item.Password*@
                                    </td>
                                    <td class="text-primary">
                                        <a href="@Url.Action("DeleteCus", "Customer")" class="btn btn-primary pull-right btn-sm">Delete</a>
                                    </td>
                                </tr>
                            


                        </tbody>
                    </table>
                </div>
            </div>
        </div>
        @*<div class="row">
            <div class="col-sm-12">
                <div class="pagination font-alt">
                    <div class="numberpage">Page @(view.PageCount < view.PageNumber ? 0 : view.PageNumber) / @view.PageCount</div>
                    <div class="page">
                        @Html.PagedListPager(view, page => Url.Action("ViewCus",
                                       new { page }))
                    </div>
                </div>
            </div>*@
        </div>
    @*  *@


</div>
<style>
    .pagination {
        display: flex;
        flex-direction: row;
        justify-content: space-around;
    }

        .pagination > li {
            width: 25px;
            height: 25px;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
            border: 1px solid;
            border-radius: 10%;
            background-color: rebeccapurple;
        }

    .page {
        width: 350px;
    }

    .numberpage {
        width: 155px;
        height: 25px;
        display: flex;
        justify-content: center;
        align-items: center;
        text-align: center;
        float: inherit;
    }
</style>

